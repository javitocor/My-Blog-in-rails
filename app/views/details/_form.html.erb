<%= simple_form_for([@user, @user.detail.build]) do |f| %>
<!--
collection.build(attributes = {}, …) Returns one or more new objects of the collection type that have been instantiated with attributes and linked to this object through a foreign key, but have not yet been saved. Note: This only works if an associated object already exists, not if it‘s nil!
-->
<div>
    <div class="form-group">
        <%= f.input :nationality, wrapper: false, label_html: { class:"form-control" }, required: false %>
    </div>
    <div class="form-group">
        <%= f.input :definition, wrapper: false, label_html: { class:"form-control" }, required: false  %>
    </div>
    <div class="form-group">
        <%= f.input :birthdate, as: :date, wrapper: false, label_html: { class:"form-control" }, required: false  %>
    </div>
    <div class="form-group">
        <%= f.input :employee, as: :boolean, inline_label: 'Mark it if you are working', wrapper: false, label_html: { class:"form-control" }, required: false  %>
    </div>
    <div class="form-group">
        <%= f.input :income, as: :float, wrapper: false, label_html: { class:"form-control" }, required: false  %>
    </div>
</div>
<%= f.button :submit, 'Update', class:"btn btn-primary" %>
<% end %>